# Normalise to LF in the repo. Without this, committing from Windows stores
# CRLF, and the Alpine container then fails on shell scripts and diffs get
# noisy for anyone on Linux or macOS.
* text=auto eol=lf

# Keep binaries byte-exact.
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
